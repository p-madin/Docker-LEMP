[mysqld]
# Explicitly set the pool size
innodb_buffer_pool_size = 128M

# CRITICAL: Disable this to save ~150MB+ of RAM on small VMs
performance_schema = OFF

# Reduce per-connection memory overhead
max_connections = 20
innodb_log_buffer_size = 4M